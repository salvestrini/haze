autogen definitions sitetool;

common = {
	layout = "./layout.sxml";
};

vars = {
	var-sitename = "Filter Chains Processor";
};

include "contents.as"
include "pages.as"
include "map.as"
include "files.as"
